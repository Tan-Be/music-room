# 🔧 Устранение проблем с регистрацией

## ❌ Проблема: Не получается зарегистрироваться

### Шаг 1: Выполните SQL скрипт в Supabase

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **SQL Editor** (слева в меню)
4. Создайте новый запрос
5. Скопируйте и вставьте содержимое файла `docs/supabase-profile-trigger.sql`
6. Нажмите **Run** (или F5)

Это создаст:
- ✅ Триггер для автоматического создания профиля
- ✅ RLS политики для безопасности
- ✅ Индексы для производительности

### Шаг 2: Проверьте таблицу profiles

1. В Supabase Dashboard перейдите в **Table Editor**
2. Найдите таблицу `profiles`
3. Убедитесь, что она существует и имеет поля:
   - `id` (uuid, primary key)
   - `username` (text)
   - `avatar_url` (text, nullable)
   - `spotify_id` (text, nullable)
   - `tracks_added_today` (integer)
   - `last_track_date` (date)
   - `created_at` (timestamp)
   - `updated_at` (timestamp)

### Шаг 3: Проверьте RLS политики

1. В Table Editor выберите таблицу `profiles`
2. Перейдите на вкладку **Policies**
3. Должны быть политики:
   - ✅ "Profiles are viewable by everyone" (SELECT)
   - ✅ "Users can update own profile" (UPDATE)
   - ✅ "Users can insert own profile" (INSERT)

### Шаг 4: Проверьте Email настройки

1. В Supabase Dashboard перейдите в **Authentication** → **Settings**
2. Убедитесь, что:
   - ✅ **Enable email confirmations** - ВЫКЛЮЧЕНО (для тестирования)
   - ✅ **Enable email provider** - ВКЛЮЧЕНО

Для тестирования отключите подтверждение email:
```
Authentication → Settings → Email Auth → 
Disable "Enable email confirmations"
```

### Шаг 5: Очистите кеш и перезапустите

```bash
# Остановите dev сервер (Ctrl+C)

# Удалите кеш
Remove-Item -Recurse -Force .next

# Запустите заново
npm run dev
```

### Шаг 6: Попробуйте зарегистрироваться

1. Откройте `http://localhost:3000/register`
2. Заполните форму:
   - **Username:** минимум 3 символа (например: `testuser`)
   - **Email:** валидный email (например: `test@example.com`)
   - **Password:** минимум 6 символов (например: `password123`)
   - **Confirm Password:** повторите пароль
3. Нажмите "Зарегистрироваться"

## 🔍 Диагностика ошибок

### Ошибка: "User already registered"
**Решение:** Используйте другой email или войдите через `/login`

### Ошибка: "Invalid email"
**Решение:** Проверьте формат email (должен содержать @)

### Ошибка: "Password should be at least 6 characters"
**Решение:** Используйте пароль длиннее 6 символов

### Ошибка: "Failed to create profile"
**Решение:** 
1. Выполните SQL скрипт из Шага 1
2. Проверьте RLS политики
3. Убедитесь, что таблица `profiles` существует

### Ошибка: "Email not confirmed"
**Решение:** 
1. Отключите подтверждение email в настройках Supabase
2. Или проверьте почту и подтвердите email

### Ошибка в консоли браузера
1. Откройте DevTools (F12)
2. Перейдите на вкладку **Console**
3. Скопируйте текст ошибки
4. Отправьте мне для диагностики

## 🎯 Альтернативный способ: OAuth

Если регистрация через email не работает, попробуйте:

### Вход через Google
1. На странице `/register` нажмите "Войти через Google"
2. Выберите аккаунт Google
3. Разрешите доступ
4. Вы будете автоматически зарегистрированы

### Вход через GitHub
1. На странице `/register` нажмите "Войти через GitHub"
2. Авторизуйтесь в GitHub
3. Разрешите доступ
4. Вы будете автоматически зарегистрированы

## 📝 Проверка логов

### В терминале (где запущен npm run dev)
Ищите ошибки типа:
```
Error: Cannot find module...
Error: Invalid Supabase URL...
Error: Missing Supabase anon key...
```

### В браузере (F12 → Console)
Ищите ошибки типа:
```
Registration error: ...
Supabase error: ...
Network error: ...
```

## 🆘 Если ничего не помогло

1. Проверьте `.env.local`:
```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

2. Убедитесь, что Supabase проект активен

3. Проверьте интернет-соединение

4. Попробуйте в режиме инкогнито

5. Отправьте мне:
   - Текст ошибки из консоли
   - Скриншот страницы регистрации
   - Логи из терминала

---

**Нужна помощь?** Отправьте мне детали ошибки! 🚀
