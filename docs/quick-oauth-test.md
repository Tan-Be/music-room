# ⚡ Быстрая проверка OAuth кнопок

## 🔍 Что проверить

### 1. Кнопки отображаются?

Откройте `http://localhost:3000/login` или `http://localhost:3000/register`

Должны быть видны 3 кнопки:

- ✅ **Войти через Google** (с цветным логотипом Google)
- ✅ **Войти через GitHub** (с чёрным логотипом GitHub)
- ✅ **Войти через Spotify** (с зелёной иконкой музыки)

### 2. Кнопки кликабельны?

При наведении:

- Google: фон становится светло-синим
- GitHub: фон становится чёрным, текст белым
- Spotify: без изменений

### 3. Что происходит при клике?

#### Если OAuth НЕ настроен:

```
❌ Ошибка в консоли браузера (F12):
"OAuth provider not configured"
или
"Invalid client credentials"
```

#### Если OAuth настроен:

```
✅ Перенаправление на страницу авторизации:
- Google: accounts.google.com
- GitHub: github.com/login/oauth
- Spotify: accounts.spotify.com
```

---

## 🧪 Тест без настройки OAuth

Если OAuth не настроен, кнопки всё равно должны:

1. ✅ Отображаться
2. ✅ Быть кликабельными
3. ✅ Показывать "Вход..." при клике
4. ❌ Показывать ошибку "Не удалось войти через [провайдер]"

Это нормально! Просто нужно настроить OAuth в Supabase.

---

## 🔧 Если кнопки не работают

### Проверьте консоль браузера (F12)

Ищите ошибки:

```javascript
// Хорошо - OAuth не настроен, но код работает
'OAuth provider not configured'

// Плохо - проблема с кодом
"Cannot read property 'signInWithOAuth'"
'supabase is not defined'
```

### Проверьте Supabase URL

В `.env.local` должно быть:

```env
NEXT_PUBLIC_SUPABASE_URL=https://syxjqxfoycmttcmrasgq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...
```

### Перезапустите dev сервер

```bash
npm run dev
```

---

## ✅ Кнопки работают правильно, если:

1. Они отображаются на странице
2. При клике показывается "Вход..."
3. Появляется toast уведомление (успех или ошибка)
4. В консоли нет ошибок JavaScript

**Если всё это работает, значит код правильный!**  
Просто нужно настроить OAuth провайдеры в Supabase.

---

## 📚 Следующий шаг

Следуйте инструкции в `docs/oauth-providers-setup.md` для настройки:

- Google OAuth
- GitHub OAuth
- Spotify OAuth (опционально)
