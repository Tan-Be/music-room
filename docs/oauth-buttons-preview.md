# 🎨 Превью OAuth кнопок

## Как выглядят кнопки входа

### На странице входа (/login)

```
┌─────────────────────────────────────────┐
│         Вход в Music Room               │
│                                         │
│  Email: [________________]              │
│  Пароль: [________________]             │
│                                         │
│  [        Войти        ]                │
│                                         │
│  ─────── Или продолжить с ──────        │
│                                         │
│  [🔵 Войти через Google    ]            │
│  [⚫ Войти через GitHub    ]            │
│  [🟢 Войти через Spotify   ]            │
│                                         │
│  Нет аккаунта? Зарегистрируйтесь       │
└─────────────────────────────────────────┘
```

## 🎨 Стили кнопок

### Google Button

- **Цвет**: Белый фон с цветным логотипом Google
- **Hover**: Светло-синий фон
- **Иконка**: Официальный логотип Google (4 цвета)

### GitHub Button

- **Цвет**: Белый фон с черной иконкой
- **Hover**: Черный фон с белым текстом
- **Иконка**: Логотип GitHub (Octocat)

### Spotify Button

- **Цвет**: Белый фон с зеленой иконкой
- **Hover**: Светло-зеленый фон
- **Иконка**: Логотип Spotify (нота)

## 📱 Адаптивность

### Десктоп (> 768px)

- Кнопки занимают полную ширину карточки
- Высота: 40px
- Отступы между кнопками: 12px

### Мобильные (< 768px)

- Кнопки занимают полную ширину экрана
- Высота: 48px (больше для удобства нажатия)
- Отступы между кнопками: 12px

## 🎯 Интерактивность

### Состояния кнопок:

1. **Default** - обычное состояние
2. **Hover** - при наведении курсора
   - Изменение цвета фона
   - Изменение цвета границы
   - Плавная анимация (200ms)
3. **Active** - при клике
   - Небольшое уменьшение (scale-95)
4. **Loading** - во время входа
   - Текст меняется на "Вход..."
   - Кнопка становится неактивной

## 💡 Особенности реализации

### Google

```tsx
<Button variant="outline" className="hover:bg-blue-50 hover:border-blue-500">
  <GoogleIcon /> Войти через Google
</Button>
```

### GitHub

```tsx
<Button variant="outline" className="hover:bg-gray-900 hover:text-white">
  <GithubIcon /> Войти через GitHub
</Button>
```

### Spotify

```tsx
<Button variant="outline" className="hover:bg-green-50 hover:border-green-500">
  <SpotifyIcon /> Войти через Spotify
</Button>
```

## 🔄 Процесс входа

1. Пользователь нажимает на кнопку
2. Кнопка показывает состояние загрузки
3. Происходит редирект на страницу OAuth провайдера
4. Пользователь авторизуется
5. Редирект обратно на `/auth/callback`
6. Создается профиль пользователя (если первый вход)
7. Редирект на `/rooms`

## ✅ Преимущества

- **Быстрый вход** - один клик вместо заполнения формы
- **Безопасность** - OAuth 2.0 стандарт
- **Удобство** - не нужно запоминать пароль
- **Доверие** - пользователи доверяют известным провайдерам

---

**Компоненты**:

- `src/components/auth/google-login-button.tsx`
- `src/components/auth/github-login-button.tsx`
- `src/components/auth/spotify-login-button.tsx`
