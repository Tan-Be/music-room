# 🔑 Создание реального GitHub OAuth приложения

## 📋 Пошаговая инструкция (3 минуты)

### Шаг 1: Открыть GitHub Developer Settings
1. Перейти по ссылке: **https://github.com/settings/developers**
2. Войти в свой GitHub аккаунт (если не вошли)

### Шаг 2: Создать новое OAuth приложение
1. Нажать кнопку **"New OAuth App"** (зеленая кнопка справа)

### Шаг 3: Заполнить форму ТОЧНО так:
```
Application name: Music Room Local Dev
Homepage URL: http://localhost:3000
Application description: Music Room - платформа для совместного прослушивания музыки
Authorization callback URL: http://localhost:3000/api/auth/callback/github
```

⚠️ **ВАЖНО**: Callback URL должен быть ТОЧНО `http://localhost:3000/api/auth/callback/github`

### Шаг 4: Зарегистрировать приложение
1. Нажать **"Register application"**
2. GitHub создаст приложение и покажет страницу с данными

### Шаг 5: Получить Client ID и Secret
1. **Client ID** - сразу виден на странице (например: `Ov23liABC123DEF456`)
2. **Client Secret** - нажать **"Generate a new client secret"**
3. Скопировать оба значения (Secret показывается только один раз!)

## 🔧 Обновление конфигурации

### Автоматический способ (рекомендуется):
```powershell
# В PowerShell (в корне проекта)
.\scripts\setup-github-oauth.ps1 "ВАШ_CLIENT_ID" "ВАШ_CLIENT_SECRET"
```

### Ручной способ:
Открыть `.env.local` и заменить:
```bash
NEXT_PUBLIC_GITHUB_CLIENT_ID=ваш_реальный_client_id
GITHUB_CLIENT_ID=ваш_реальный_client_id  
GITHUB_CLIENT_SECRET=ваш_реальный_client_secret
```

## 🔄 Перезапуск сервера
```bash
# Остановить сервер: Ctrl+C
# Запустить заново:
pnpm dev
```

## ✅ Проверка работы

1. Открыть http://localhost:3000/auth/signin
2. Нажать **"Войти через GitHub"**
3. Должен открыться реальный GitHub OAuth экран
4. После авторизации - возврат в приложение с активной сессией

## 🎯 Результат

После настройки:
- ✅ Полноценный вход через GitHub
- ✅ Создание профиля пользователя
- ✅ Сохранение сессии
- ✅ Все функции аутентификации работают

## 🔍 Устранение проблем

### Если GitHub показывает ошибку:
- Проверить Callback URL: `http://localhost:3000/api/auth/callback/github`
- Убедиться, что сервер запущен на порту 3000

### Если кнопка не активна:
- Проверить, что Client ID не содержит `demo_client_id`
- Перезапустить сервер после изменения .env.local

### Если ошибка после авторизации:
- Проверить Client Secret в .env.local
- Убедиться, что NextAuth.js настроен правильно

## 📱 Для продакшена

Когда будете деплоить на Vercel:
1. Создать отдельное GitHub OAuth приложение
2. Callback URL: `https://ваш-домен.vercel.app/api/auth/callback/github`
3. Добавить переменные в Vercel Environment Variables