# 🎵 Фоновая музыка - Инструкция

## ✅ Что реализовано

На главной странице добавлена фоновая музыка с красивым плеером в правом нижнем углу.

### Функции:
- ✅ **Play/Pause** - включение/выключение музыки
- ✅ **Mute/Unmute** - отключение звука
- ✅ **Автоповтор** - музыка играет по кругу
- ✅ **Громкость 30%** - комфортная громкость
- ✅ **Анимация** - визуализация воспроизведения
- ✅ **Подсказка** - при первом посещении

## 🎼 Как добавить свою музыку

### Вариант 1: Локальный файл (рекомендуется)

1. **Создайте папку для музыки:**
```bash
mkdir public/music
```

2. **Добавьте музыкальный файл:**
   - Поместите файл `background-music.mp3` в `public/music/`
   - Формат: MP3, OGG или WAV
   - Размер: желательно до 5MB
   - Длительность: 2-5 минут (будет повторяться)

3. **Обновите путь в компоненте:**

Откройте `src/components/common/background-music.tsx` и измените:

```typescript
// Было:
audio.src = 'https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3'

// Стало:
audio.src = '/music/background-music.mp3'
```

### Вариант 2: Внешний URL

Используйте прямую ссылку на музыку:

```typescript
audio.src = 'https://your-cdn.com/music.mp3'
```

### Вариант 3: Несколько треков

Создайте плейлист:

```typescript
const playlist = [
  '/music/track1.mp3',
  '/music/track2.mp3',
  '/music/track3.mp3',
]

let currentTrack = 0

audio.src = playlist[currentTrack]
audio.addEventListener('ended', () => {
  currentTrack = (currentTrack + 1) % playlist.length
  audio.src = playlist[currentTrack]
  audio.play()
})
```

## 🎨 Рекомендации по музыке

### Жанры для фона:
- 🎹 **Lofi Hip-Hop** - расслабляющий, не отвлекает
- 🎺 **Jazz** - элегантный, атмосферный
- 🎻 **Classical** - спокойный, приятный
- 🌊 **Ambient** - фоновый, ненавязчивый
- 🎸 **Acoustic** - мягкий, уютный

### Где найти бесплатную музыку:

1. **YouTube Audio Library**
   - [https://www.youtube.com/audiolibrary](https://www.youtube.com/audiolibrary)
   - Бесплатно, без авторских прав

2. **Free Music Archive**
   - [https://freemusicarchive.org](https://freemusicarchive.org)
   - Большой выбор жанров

3. **Incompetech**
   - [https://incompetech.com/music](https://incompetech.com/music)
   - Музыка Kevin MacLeod

4. **Bensound**
   - [https://www.bensound.com](https://www.bensound.com)
   - Качественные треки

## ⚙️ Настройки

### Изменить громкость:

```typescript
audio.volume = 0.3 // 30% (от 0.0 до 1.0)
```

### Отключить автоповтор:

```typescript
audio.loop = false // Музыка остановится после окончания
```

### Автоматический запуск:

```typescript
useEffect(() => {
  // Автоматически запускать музыку при загрузке
  const audio = audioRef.current
  if (audio) {
    audio.play().catch(err => {
      console.log('Автозапуск заблокирован браузером')
    })
    setIsPlaying(true)
  }
}, [])
```

**Примечание:** Большинство браузеров блокируют автозапуск звука до взаимодействия пользователя.

## 🎯 Где отображается

Плеер находится в **правом нижнем углу** на главной странице (`/`).

Если хотите добавить на другие страницы:

1. Откройте нужную страницу (например, `src/app/rooms/page.tsx`)
2. Добавьте импорт:
```typescript
import { BackgroundMusic } from '@/components/common/background-music'
```
3. Добавьте компонент:
```typescript
<BackgroundMusic />
```

## 🎨 Кастомизация внешнего вида

### Изменить позицию:

```typescript
// В src/components/common/background-music.tsx
<div className="fixed bottom-4 right-4 z-50"> // Правый нижний угол
<div className="fixed top-4 right-4 z-50">    // Правый верхний угол
<div className="fixed bottom-4 left-4 z-50">  // Левый нижний угол
```

### Изменить цвета:

```typescript
className="bg-background/80 backdrop-blur-md border-2 border-primary/20"
// Измените border-primary/20 на другой цвет
```

### Убрать подсказку:

Удалите блок:
```typescript
{!hasInteracted && (
  <div className="absolute bottom-full...">
    ...
  </div>
)}
```

## 🔧 Устранение проблем

### Музыка не играет

1. **Проверьте путь к файлу:**
   - Файл должен быть в `public/music/`
   - Путь должен начинаться с `/music/`

2. **Проверьте формат:**
   - Используйте MP3 (лучшая совместимость)
   - Проверьте, что файл не повреждён

3. **Проверьте консоль браузера (F12):**
   - Ищите ошибки загрузки файла
   - Проверьте сетевые запросы

### Музыка слишком громкая/тихая

Измените значение `volume`:
```typescript
audio.volume = 0.2 // Тише (20%)
audio.volume = 0.5 // Громче (50%)
```

### Кнопка не отображается

1. Проверьте, что компонент добавлен на страницу
2. Проверьте z-index (должен быть `z-50`)
3. Убедитесь, что нет конфликтов стилей

## 📝 Пример использования

```typescript
// src/app/page.tsx
import { BackgroundMusic } from '@/components/common/background-music'

export default function Home() {
  return (
    <main>
      {/* Ваш контент */}
      
      {/* Фоновая музыка */}
      <BackgroundMusic />
    </main>
  )
}
```

## 🎵 Рекомендуемые треки

Для Music Room подойдут:
- **Lofi Beats** - расслабляющие биты
- **Chill Jazz** - спокойный джаз
- **Ambient Electronic** - электронная музыка
- **Acoustic Guitar** - акустическая гитара

**Длительность:** 2-5 минут (с loop)  
**Темп:** 80-120 BPM  
**Настроение:** Спокойное, позитивное

---

**Приятного прослушивания!** 🎧
