# 🔧 Исправление ошибки 404

## ✅ Что было исправлено

Ошибка 404 возникала из-за неправильных ссылок на главной странице.

### Исправленные ссылки:

| Было | Стало | Описание |
|------|-------|----------|
| `/auth` | `/login` | Страница входа |
| `/profile` | `/rooms` | Все комнаты |
| `/docs` | `/rooms/create` | Создание комнаты |

### Доступные страницы:

✅ **Главная:** `/` - главная страница  
✅ **Вход:** `/login` - страница входа  
✅ **Регистрация:** `/register` - страница регистрации  
✅ **Комнаты:** `/rooms` - список всех комнат  
✅ **Создать комнату:** `/rooms/create` - создание новой комнаты  
✅ **Комната:** `/room/[id]` - страница конкретной комнаты  
✅ **OAuth Callback:** `/auth/callback` - обработка OAuth

## 🚀 Как использовать

### 1. Регистрация

```
http://localhost:3000/register
```

Заполните форму:
- Username: минимум 3 символа
- Email: валидный email
- Password: минимум 6 символов
- Confirm Password: повторите пароль

### 2. Вход

```
http://localhost:3000/login
```

Введите:
- Email
- Password

Или используйте OAuth:
- Войти через Google
- Войти через GitHub
- Войти через Spotify

### 3. Просмотр комнат

```
http://localhost:3000/rooms
```

Здесь вы увидите все доступные комнаты.

### 4. Создание комнаты

```
http://localhost:3000/rooms/create
```

Заполните форму создания комнаты.

### 5. Вход в комнату

```
http://localhost:3000/room/[id]
```

Где `[id]` - это ID комнаты (например: `room/123`)

## 🔍 Если всё ещё 404

### Проверьте dev сервер

Убедитесь, что сервер запущен:
```bash
npm run dev
```

Должно быть:
```
✓ Ready in 2.5s
○ Local:   http://localhost:3000
```

### Очистите кеш

```bash
Remove-Item -Recurse -Force .next
npm run dev
```

### Проверьте URL

Убедитесь, что используете правильный URL:
- ✅ `http://localhost:3000/login`
- ❌ `http://localhost:3000/auth`

## 📝 Навигация по приложению

```
Главная (/)
├── Войти (/login)
│   └── OAuth Callback (/auth/callback)
├── Регистрация (/register)
│   └── OAuth Callback (/auth/callback)
├── Комнаты (/rooms)
│   ├── Создать комнату (/rooms/create)
│   └── Комната (/room/[id])
```

---

**Теперь все ссылки работают!** ✅
