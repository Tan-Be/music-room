# 🔧 Исправление ошибки сборки Next.js

## ❌ Ошибка

```
Error: Cannot find module './739.js'
```

## ✅ Решение

Эта ошибка возникает из-за устаревшего кеша Next.js после изменения файлов.

### Шаг 1: Остановите dev сервер

Нажмите `Ctrl+C` в терминале, где запущен `npm run dev`

### Шаг 2: Удалите кеш

```bash
# Удалите папку .next
Remove-Item -Recurse -Force .next

# Удалите кеш node_modules (опционально)
Remove-Item -Recurse -Force node_modules\.cache
```

### Шаг 3: Запустите заново

```bash
npm run dev
```

## 🎉 Готово!

Теперь приложение должно работать без ошибок.

---

**Когда использовать:**
- После изменения структуры компонентов
- После обновления зависимостей
- При странных ошибках сборки
- После переключения веток в Git

**Быстрая команда:**
```bash
Remove-Item -Recurse -Force .next ; npm run dev
```
