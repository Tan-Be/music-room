# 🚀 Руководство по деплою на Vercel

## 📋 Подготовка к деплою

### 1. Проверка готовности проекта

✅ **Проект готов к деплою:**

- Сборка проходит успешно (`pnpm run build`)
- Все тесты проходят
- Нет критических ошибок ESLint
- Все переменные окружения настроены

### 2. Переменные окружения

Следующие переменные должны быть настроены в Vercel:

#### Обязательные переменные:

```bash
NEXT_PUBLIC_SUPABASE_URL=https://syxjqxfoycmttcmrasgq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

#### Опциональные переменные:

```bash
SPOTIFY_CLIENT_ID=your-spotify-client-id
SPOTIFY_CLIENT_SECRET=your-spotify-client-secret
Github_OAuth_secret=f26fb6054d63fa3f01791e2b76b34279bf6e069f
```

## 🔧 Настройка Vercel

### Способ 1: Через Vercel CLI

1. **Установка Vercel CLI:**

```bash
npm i -g vercel
```

2. **Логин в Vercel:**

```bash
vercel login
```

3. **Деплой проекта:**

```bash
vercel --prod
```

### Способ 2: Через GitHub Integration

1. **Подключение к GitHub:**
   - Зайдите на [vercel.com](https://vercel.com)
   - Нажмите "New Project"
   - Выберите репозиторий GitHub

2. **Настройка проекта:**
   - Framework Preset: `Next.js`
   - Build Command: `pnpm run build`
   - Output Directory: `.next`
   - Install Command: `pnpm install`

3. **Добавление переменных окружения:**
   - Перейдите в Settings → Environment Variables
   - Добавьте все необходимые переменные

## 🌐 Настройка домена

### Пользовательский домен

1. **В настройках проекта Vercel:**
   - Перейдите в Settings → Domains
   - Добавьте ваш домен
   - Настройте DNS записи согласно инструкциям

2. **DNS настройки:**

```
Type: CNAME
Name: www (или @)
Value: cname.vercel-dns.com
```

### Vercel домен

По умолчанию проект будет доступен по адресу:
`https://your-project-name.vercel.app`

## 📊 Мониторинг и аналитика

### Vercel Analytics

```bash
npm i @vercel/analytics
```

Добавьте в `src/app/layout.tsx`:

```tsx
import { Analytics } from '@vercel/analytics/react'

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="ru">
      <body>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
```

### Vercel Speed Insights

```bash
npm i @vercel/speed-insights
```

## 🔍 Проверка деплоя

### Чек-лист после деплоя:

- [ ] Сайт открывается без ошибок
- [ ] Аутентификация работает
- [ ] Supabase подключение активно
- [ ] PWA устанавливается
- [ ] Service Worker работает
- [ ] Уведомления функционируют
- [ ] Мобильная версия корректна
- [ ] Все страницы доступны

### Тестирование производительности:

1. **Lighthouse аудит:**
   - Performance > 90
   - Accessibility > 95
   - Best Practices > 90
   - SEO > 90

2. **Core Web Vitals:**
   - LCP < 2.5s
   - FID < 100ms
   - CLS < 0.1

## 🚨 Устранение проблем

### Частые ошибки:

1. **Build Error: Environment variables not found**
   - Проверьте настройки переменных в Vercel
   - Убедитесь, что все переменные добавлены

2. **Supabase connection failed**
   - Проверьте URL и ключи Supabase
   - Убедитесь в корректности RLS политик

3. **PWA не устанавливается**
   - Проверьте manifest.json
   - Убедитесь, что Service Worker загружается

4. **OAuth не работает**
   - Добавьте домен Vercel в настройки OAuth провайдеров
   - Проверьте redirect URLs

### Логи и отладка:

1. **Vercel Function Logs:**
   - Перейдите в Dashboard → Functions
   - Просмотрите логи выполнения

2. **Real-time Logs:**

```bash
vercel logs --follow
```

## 📈 Оптимизация

### Настройки производительности:

1. **Кэширование:**
   - Статические ресурсы кэшируются автоматически
   - API routes кэшируются согласно headers

2. **Сжатие:**
   - Gzip/Brotli включены по умолчанию
   - Изображения оптимизируются автоматически

3. **Edge Functions:**
   - Middleware выполняется на Edge
   - Быстрый отклик по всему миру

## 🔄 CI/CD Pipeline

### Автоматический деплой:

1. **Production деплой:**
   - Каждый push в `main` ветку
   - Автоматические тесты перед деплоем

2. **Preview деплой:**
   - Каждый Pull Request
   - Уникальный URL для тестирования

3. **Rollback:**

```bash
vercel rollback [deployment-url]
```

## 📞 Поддержка

### Полезные ссылки:

- [Vercel Documentation](https://vercel.com/docs)
- [Next.js Deployment](https://nextjs.org/docs/deployment)
- [Supabase + Vercel Guide](https://supabase.com/docs/guides/getting-started/tutorials/with-nextjs)

### Команды для отладки:

```bash
# Проверка статуса деплоя
vercel ls

# Просмотр логов
vercel logs

# Информация о проекте
vercel inspect

# Удаление деплоя
vercel remove [deployment-url]
```

---

**Статус**: ✅ Готово к деплою  
**Дата**: 19.12.2024  
**Версия**: 1.0
